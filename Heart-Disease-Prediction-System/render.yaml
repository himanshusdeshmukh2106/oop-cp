services:
  - type: web
    name: heart-disease-prediction
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn health_desease.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: False
      - key: ALLOWED_HOSTS
        sync: false
